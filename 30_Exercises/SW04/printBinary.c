#include <stdio.h>
#include <stdlib.h>

int PrintBinary ( int zahl ) //Funktion?
{
	
	return result;
}

int main (int argc, char* argv[])
{

	int input = atoi(argv[1]);
	long result =0;
	result = PrintBinary(input);
	
	printf("%d in bynary form ist %ld \n", input, result); //ld?
	return 0;
}
